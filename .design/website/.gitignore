# The seeded canvas page — a ~2.5 MB generated bundle carrying the editor
# itself. The design lives at its published URL; the artboards beside this
# file are the source it is rebuilt from.
sawa-cars-website.html

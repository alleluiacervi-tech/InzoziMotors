# Backend Gap Audit — Remaining Implementations (excl. Phase 8 integrations)

> Generated Jul 19 2026 by a 38-agent verified audit: 5 parallel inventories
> (routes, mobile expectations, admin dashboard, schema, blueprint) →
> cross-reference → per-gap adversarial verification against the code.
> **All 32 gaps below were CONFIRMED with file-level evidence; 0 refuted.**
>
> **STATUS UPDATE (Jul 19 2026):** Tiers 1–3 implemented and smoke-tested (35 checks
> passing) in commits 7ad2ead + follow-up. Remaining open items: multipart uploads with
> real files end-to-end, admin dashboard click-test, mobile screens for referrals/disputes
> wiring, dealer accounts backend, notification push delivery (OneSignal — Phase 8),
> 36-angle car_photos write path (table exists; upload route still writes cars.images).

## HIGH — broken in practice, security, or business-critical

### 1. Static /uploads serves KYC identity documents with no auth  `[backend-route]`
**Evidence:** backend/server.js:32-33 — app.use('/uploads', express.static(uploadDir)) with no middleware; POST /id-verification (src/routes/id-verification.js:21-23) writes national ID front/back + selfie URLs under /uploads/id-docs/, so anyone with a URL can view identity documents. Needs an authenticated/admin-gated file route (or signed URLs) before ID verification can go live.

**Verified:** Confirmed. backend/server.js:32-33 mounts app.use('/uploads', express.static(uploadDir)) with no auth middleware, serving the whole uploads dir publicly. backend/src/routes/id-verification.js:21-23 writes national ID front/back + selfie under /uploads/id-docs/, so those KYC files are reachable by anyone with the URL. Filenames (backend/src/middleware/upload.js) are Date.now() + Math.random().toString(36) — not a signed-URL or CSPRNG scheme. Refutation searches across backend/src, admin/src, src/api (does not exist yet), and src/context found no authenticated file route, signed/presigned URL logic, sendFile-with-auth, X-Accel-Redirect, or Nginx-level gate; src/context/AppContext.js:690-695 is an explicit mock upload. An admin-gated file route or signed URLs is genuinely missing before ID verification can go live.

### 2. ID verification storage is an explicit stub (docs stashed in notifications.meta) and the admin queue shape breaks the /users page  `[backend-route]`
**Evidence:** src/routes/id-verification.js:31 comment 'use notifications meta for now'; doc URLs stored as JSON in a notifications row and the queue does a hacky LEFT JOIN on n.meta->>'userId' (line 53). Meanwhile schema.sql users table already has id_front_url/id_back_url/selfie_url/id_submitted_at columns that are never written, and admin/src/app/(admin)/users/page.tsx:74-76 renders u.id_front_url etc. — fields the queue never returns, so admins approve/reject IDs completely blind. Fix: write the users columns (or a verifications table), return flat URLs + phone + id_submitted_at from GET /id-verification/queue, dedupe resubmissions.

**Verified:** Gap confirmed on all counts. backend/src/routes/id-verification.js:31 has the literal comment 'use notifications meta for now' and stashes doc URLs as JSON in a notifications row (lines 32-37); the admin queue (line 53) LEFT JOINs on n.meta->>'userId' and returns nested `docs` JSON plus `submitted_at` — never the flat id_front_url/id_back_url/selfie_url/phone/id_submitted_at fields. schema.sql defines those users columns (lines 14-17, ALTERs 199-202) but grep shows zero writers anywhere in backend/src. admin/src/app/(admin)/users/page.tsx:74-94 conditionally renders u.id_front_url/u.id_back_url/u.selfie_url, which are always undefined, so no document links ever render and admins approve/reject blind; the API client (admin/src/lib/api.ts:73) is a raw passthrough with no shape adaptation. Resubmissions insert additional notification rows while the user stays 'pending', so the JOIN duplicates queue entries — confirming the dedupe need. The mobile app (src/api/auth.js:28-44) posts to the same stub route; no verifications table or alternate implementation exists.

### 3. Handover state model mismatch makes confirmed handovers invisible in admin, plus no confirm guard/transaction  `[backend-route]`
**Evidence:** PATCH /handovers/:id/confirm (src/routes/handovers.js:121) sets status='complete', but admin/handovers/page.tsx:13,51 has a 'confirmed' tab querying ?status=confirmed — permanently empty. Confirm also has no current-status guard (re-confirming or confirming a cancelled handover double-increments users.completed_sales) and runs 6 writes with no transaction. OrderTrackingScreen expects Request→Confirmed→Handover→Complete but schema only has pending|complete|cancelled. Needs: status alignment (or an intermediate 'confirmed' state), idempotent confirm, transaction.

**Verified:** Gap is real, verified in code. (1) backend/src/routes/handovers.js:121 sets status='complete' on confirm, schema.sql:111 documents pending|complete|cancelled, yet admin/src/app/(admin)/handovers/page.tsx:13,51 queries a 'confirmed' tab via admin/src/lib/api.ts:58 (?status=confirmed passed literally to WHERE h.status=$1 at handovers.js:102) — no code ever writes 'confirmed' and no mapping layer exists (repo-wide grep hits only mobile mock AppContext.js), so the tab is permanently empty and completed handovers are invisible in admin. (2) Confirm handler (handovers.js:113-134) has no current-status guard: SELECT and UPDATE both lack status conditions, so re-confirming or confirming a cancelled handover double-increments users.completed_sales and re-marks a relisted car sold. (3) Zero transactions in backend — grep for BEGIN/COMMIT/ROLLBACK across backend/src returns nothing; confirm runs 5 sequential writes (UPDATE handovers/cars/users + 2 notification INSERTs) on pool.query with no atomicity. (4) OrderTrackingScreen.js:45 models 4 stages (reserved→booked→handover→complete, 'booked' styled as Confirmed) that the 3-state backend schema cannot represent. Claim's only imprecisions are trivial: "6 writes" is 5 writes + 1 SELECT, and the mobile stage names are paraphrased.

### 4. Admin /submissions flow broken: 'pending' default tab is always empty, Schedule button never creates the inspection row, dates render Invalid Date  `[admin]`
**Evidence:** admin/submissions/page.tsx defaults to status 'pending' which no backend code ever writes (submissions are created 'under_review', submissions.js:20). 'Schedule Inspection' PATCHes {status:'scheduled'} with no center/date/time, and submissions.js:101 only INSERTs an inspections row 'if (status===scheduled && center)' — so the admin path never creates an appointment (only the seller's mobile self-schedule route does). Page also reads sub.created_at but the column is submitted_at. Needs a center/date/time picker wired to the existing PATCH fields, correct default tab, and field-name fix.

**Verified:** Gap confirmed on all three points; no refuting implementation found. (1) admin/src/app/(admin)/submissions/page.tsx:17 defaults the tab to 'pending', but backend/src/schema.sql:60 defaults submissions to 'under_review' and backend/src/routes/submissions.js:21 is the only INSERT (hardcoded 'under_review'); no backend code ever writes 'pending' to submissions, so the default tab is always empty — and the page's 'pending'-gated action buttons (page.tsx:106-121) are dead code. (2) The Schedule button (page.tsx:126) PATCHes only {status:'scheduled'}; api.ts:45 updateSubmission adds nothing, and submissions.js:101 only INSERTs an inspections row when center is present, so the admin path never creates an appointment — only the seller self-schedule route (submissions.js:139-165) does, and no admin page has any center/date/time picker or inspection-creation call (admin inspections page only lists/completes). (3) page.tsx:92 reads sub.created_at but the column is submitted_at (schema.sql:75), so every row renders 'Invalid Date'. The proposed fix is viable since the backend PATCH already accepts center/scheduled_date/scheduled_time (submissions.js:87).

### 5. Inspection completion engine incorrect: equal weighting, no publish threshold, submission marked 'live' with no listing, no re-complete guard  `[backend-route]`
**Evidence:** POST /inspections/:id/complete (src/routes/inspections.js:110-150) weights all items equally (pass=1/flag=0.5/fail=0) instead of the documented 25/25/20/20/20/15/25 category weights; auto-publishes the car live with no minimum score and notifies 'Your car passed' regardless; when car_id is null (the normal admin-web flow, where the listing is created afterwards) it still sets the submission to 'live' with no car, desyncing the pipeline; checklist values unvalidated; can overwrite a completed inspection; 4 writes, no transaction. Admin page compounds it: /inspections/[id] alerts 'Car is now live' when nothing was published.

**Verified:** All sub-claims verified in code. backend/src/routes/inspections.js:110-178: score uses flat pass=1/flag=0.5/fail=0 per-item weighting (lines 116-124), not the documented 25/25/20/20/20/15/25 category weights (no weighting logic anywhere in backend/src, admin/src, or src/api); car published 'live' with no minimum-score threshold and seller notified 'Your car passed... now live' at any score including Grade D (lines 139-170); submissions set to 'live' outside the if(car_id) block (lines 150-153), and in the normal admin-web flow car_id IS null at completion (inspections copy sub.car_id at scheduling in submissions.js:107-111/160-164; car_id only back-filled later by POST /cars in cars.js:188-197), so the pipeline desyncs with a 'live' submission and no car; checklist values unvalidated beyond non-empty-object (unknown values silently score 0, no item-name/count check); no re-complete guard (status never checked before overwrite, notification re-fires); 4 separate pool.query writes with zero transaction usage in any route (grep confirmed). Admin page compounds it: admin/src/app/(admin)/inspections/[id]/page.tsx:88 unconditionally alerts 'Inspection complete! Car is now live.' even when nothing was published, and its displayed score (line 101, pass/total) disagrees with the server formula. No refuting implementation found.

### 6. Unverified sellers can submit cars and reference photos are silently dropped  `[backend-route]`
**Evidence:** POST /submissions (src/routes/submissions.js) never checks users.id_verified despite 'Identity: Mandatory for sellers' (CLAUDE.md core concept), and accepts no photo fields even though CarSubmissionScreen has a Photos step and schema.sql has submissions.reference_images TEXT[] — the wizard's photos go nowhere. PATCH /submissions/:id also accepts any status string (no allowlist) and its ON CONFLICT DO NOTHING has no conflict target, so rescheduling duplicates inspection rows.

**Verified:** Gap confirmed on all points. (1) POST /submissions (backend/src/routes/submissions.js:8) is guarded only by requireAuth; the JWT payload is {id, email, role} (auth.js makeToken) and the route never reads users.id_verified — no verified-seller middleware exists anywhere in backend/src/middleware. (2) The route destructures only text fields and its INSERT omits reference_images; repo-wide, the only writer of submissions.reference_images is nonexistent (grep hits only schema.sql:74 and a permanently-null read at src/context/AppContext.js:290). CarSubmissionScreen collects photos (line 93) and passes them (line 126) but AppContext.addSubmission (lines 726-741) drops the field before calling the API, and no multer route accepts submission photos (only id-verification docs and admin car photos in inspections.js). (3) PATCH /submissions/:id sets status = req.body.status verbatim (line 95) with no allowlist, and schema.sql has no CHECK constraint on submissions.status — any string, e.g. 'live', is accepted. (4) The ON CONFLICT DO NOTHING clauses (lines 110 and 163) are no-ops because the inspections table (schema.sql:81-97) has no unique constraint other than the generated UUID PK, so every reschedule inserts a duplicate inspection row; the seller schedule route explicitly allows re-calling while already 'scheduled' (line 149), making the duplicate path reachable in normal use. No refuting implementation found in backend/src, admin/src, src/api, or src/context.

### 7. Two conflicting trust-score systems and a never-computed response_rate  `[backend-route]`
**Evidence:** Mutable users.trust_score is incremented by PATCH /id-verification/:userId (+30 on approve, no idempotency — re-approval inflates unboundedly) and by POST /reviews ((rating−3)×2 per review, no duplicate guard, schema lacks UNIQUE(reviews.handover_id)), while GET /reviews/trust-score/:userId computes the documented 30/30/20/20 breakdown independently — the two values disagree. users.response_rate is read (auth.js, reviews.js) but written by no backend code, so the 20-pt response component is permanently mock. Needs: single source of truth (computed), review dedupe constraint, a response-rate job over messages.

**Verified:** Gap confirmed on all points. (1) backend/src/routes/id-verification.js:86-91 runs `UPDATE users SET trust_score = trust_score + 30` on every 'approved' PATCH with no check of prior id_verified state and no cap — re-approval inflates the score unboundedly. (2) backend/src/routes/reviews.js:26-38 inserts a review and applies `trust_score + (rating-3)*2` with no duplicate guard, and backend/src/schema.sql:169-177 defines the reviews table with no UNIQUE(handover_id) (or handover_id+reviewer_id) constraint, so one buyer can repeatedly review the same handover. (3) GET /reviews/trust-score/:userId (reviews.js:70-105) independently computes the documented 30/30/20/20 breakdown and never reads or reconciles users.trust_score, while cars.js (lines 35/109/152) and auth.js expose the mutable column as seller_trust — the two values demonstrably disagree (new user: stored 0 vs computed 20 from the response_rate DEFAULT 100). (4) response_rate is read in auth.js:78, cars.js:110, reviews.js:73/89/98 but written by no code anywhere — the only UPDATE users statements in the backend touch completed_sales, id_verified, and trust_score; no cron/job/socket code computes it from messages, so the 20-pt response component is permanently the schema default of 100 (mock). No refuting implementation found in backend/src, admin/src, src/api (does not exist), or src/context.

### 8. Saved-search matcher and price-drop alert generators do not exist  `[backend-route]`
**Evidence:** POST/GET/PATCH/DELETE /saved-searches store filters + notify_enabled, but no code anywhere scans new/changed listings against saved searches or saved_cars — notify_enabled is a dead flag and the notifications type 'price_drop' can never fire server-side. Blueprint calls saved-search alerts the 'core re-engagement loop' (rec #12) and price-drop alerts rec #32. Blocked partly by the missing price_history table (see schema gap).

**Verified:** Confirmed. backend/src/routes/saved-searches.js is CRUD-only (store/toggle notify_enabled) and no code anywhere consumes it: every INSERT INTO notifications in backend/src (socket.js, routes/handovers.js, id-verification.js, inspections.js, messages.js, rentals.js, submissions.js) is for messages/handovers/verification/listing_update — none creates price_drop or saved-search-match notifications. POST /cars and PATCH /cars/:id/status (backend/src/routes/cars.js:170-225) never query saved_searches or saved_cars; there is no price-update endpoint at all, no price_history table in schema.sql, and no cron/setInterval/worker infrastructure in backend/. The 'price_drop' string exists server-side only as a schema comment (schema.sql:142). Client-side, src/context/AppContext.js:89-92 uses hardcoded mock matchCount/lastMatch and src/data/marketData.js:39 uses a static PRICE_DROPS map — mock, not a real matcher. notify_enabled is a dead flag; the gap is genuine.

### 9. Socket.io chat has an eavesdropping hole and is split-brained from the REST message path (never smoke-tested)  `[backend-route]`
**Evidence:** src/socket.js: join_conversation does no membership check — any authenticated user can join conv:<id> and receive both parties' messages in real time; POST /messages/conversations/:id never emits 'new_message', so REST-sent messages don't appear live for connected peers; socket.user.name is never in the JWT payload so sender_name falls back to email. CLAUDE.md 6C explicitly notes Socket.io realtime is not smoke-tested. Needs membership check on join, shared emit path, name claim, and an end-to-end test.

**Verified:** All evidence verified in code; no refuting implementation exists. (1) Eavesdropping: backend/src/socket.js:19-21 joins conv:<id> with no membership check (while send_message at lines 32-36 and the REST GET at routes/messages.js:35-39 do check, proving the join is a genuine miss); any authenticated JWT holder can join any room and receive new_message broadcasts (line 52) and typing events. (2) Split-brain: POST /messages/conversations/:id (messages.js:126-160) and POST /messages/conversations (64-123) never emit; no route references io, and server.js never shares io with Express (no app.set), so routes structurally cannot emit. Worse than claimed: mobile src/context/AppContext.js:940-957 sends each message via BOTH REST and socket send_message — both INSERT, causing DB double-writes when both are live — and the first message of a new conversation (line 918) is REST-only so a connected peer never sees it live. (3) Name claim: the only jwt.sign in the repo (backend/src/routes/auth.js:9-14) signs {id,email,role} with no name, so sender_name (socket.js:54) always falls back to email and user_typing name is undefined. (4) Untested: zero test/smoke/spec files exist in the repo, and CLAUDE.md 6C explicitly lists Socket.io realtime as not smoke-tested; admin/src has no socket usage. The gap is real and needs a membership check on join, a shared emit path (io available to routes or single write path), name in the JWT or a DB lookup, dedup of the REST+socket double-send, and an end-to-end test.

### 10. Multipart uploads never exercised end-to-end; mobile sends placeholder file URIs, PhotoUploadScreen is a demo  `[mobile-wiring]`
**Evidence:** IDVerificationScreen has no image picker — context submitIDVerification posts 'file://placeholder_id_front.jpg' etc. to POST /id-verification; PhotoUploadScreen (36-angle) makes no API call at all ('Mark as uploaded for demo?' alerts) while POST /inspections/cars/:carId/photos exists; admin /listings/[id]/photos upload page exists but CLAUDE.md notes it is 'not yet exercised'. Work: wire expo-image-picker, exercise both multer routes, verify from admin drag-drop too.

**Verified:** Gap confirmed. Mobile side is genuinely mock: expo-image-picker absent from package.json entirely; src/context/AppContext.js:697-699 posts hardcoded 'file://placeholder_*.jpg' URIs (comment: 'Mock uploads to trigger backend review pipeline') via src/api/auth.js FormData wrapper, so no real bytes reach POST /id-verification; src/screens/PhotoUploadScreen.js imports no API module and its 'Mark as uploaded for demo?' alert (line 58) only flips local state while POST /inspections/cars/:carId/photos (backend/src/routes/inspections.js:63) goes uncalled. Multer routes never exercised: backend/uploads/ has 0 files and no smoke/test script touches either upload route. One correction to the claim's framing: the admin drag-drop page is real working code, not a stub — admin/src/app/(admin)/listings/[id]/photos/page.tsx:72-120 does FileList → FormData → api.uploadCarPhotos (admin/src/lib/api.ts:85-86), so CLAUDE.md's 'NOT BUILT' note is stale; the remaining admin work is exercising/verifying it, not building it. The mobile expo-image-picker wiring and end-to-end exercise of both multer routes remain genuine outstanding work.

### 11. Mobile screens render pure mocks despite existing, working backend routes (dead api modules)  `[mobile-wiring]`
**Evidence:** src/api/inspections.js and src/api/reviews.js are imported nowhere (confirmed by grep). InspectionReportScreen always renders MOCK_INSPECTION_RESULT (src/screens/InspectionReportScreen.js:9,87) instead of GET /inspections/report/:carId; VehicleHistoryScreen uses the VEHICLE_HISTORY map instead of GET /cars/:id/history; SellerProfileScreen + TrustScoreScreen use name-keyed SELLER_PROFILES instead of GET /reviews/seller/:userId and /reviews/trust-score/:userId (and need a real seller id passed, not a name); no screen can POST /reviews at all. These are 6D-unchecked items in CLAUDE.md.

**Verified:** Gap confirmed. src/api/inspections.js and src/api/reviews.js are complete client modules (getReport, getVehicleHistory, getSellerReviews, getTrustScore, postReview) but imported nowhere — AppContext.js imports 7 other api modules but not these two. InspectionReportScreen.js:9,87 unconditionally renders MOCK_INSPECTION_RESULT despite backend/src/routes/inspections.js:41 implementing GET /inspections/report/:carId. VehicleHistoryScreen.js:8,62 uses the mock VEHICLE_HISTORY map despite backend/src/routes/cars.js:51 implementing GET /cars/:id/history. SellerProfileScreen.js:111 and TrustScoreScreen.js:146 key mock SELLER_PROFILES by seller NAME string despite backend/src/routes/reviews.js implementing /reviews/seller/:userId and /reviews/trust-score/:userId (line 70) — confirming both mock usage and the missing seller-id plumbing. No code anywhere in src/ calls POST /reviews (postReview exists only in the dead module). No direct api.get/fetch calls to these endpoints exist in screens/context/components as an alternative path. All are unchecked Phase 6D items in CLAUDE.md.

### 12. Revenue model has zero backend: no fees, commission, or featured-listing structures  `[schema]`
**Evidence:** Agreed business model (CLAUDE.md): certification fee upfront, success commission at handover confirm, featured-listing boost. schema.sql has no fees/payments/ledger table, no commission_rate/amount on handovers, no cars.featured/featured_until, and PATCH /handovers/:id/confirm records no money owed; GET /admin/stats totalRevenue is GMV (SUM of sold prices), not revenue. Needs schema + routes for fee recording and a featured flag honored in GET /cars ordering.

**Verified:** Gap confirmed. backend/src/schema.sql has no fees/payments/ledger table, no commission columns on handovers (only agreed_price), and no cars.featured/featured_until column. PATCH /handovers/:id/confirm (backend/src/routes/handovers.js:112-161) marks the car sold and notifies parties but records no fee or commission owed. GET /admin/stats totalRevenue (backend/src/routes/admin.js:19) is SUM(price) of sold cars — GMV, not platform revenue — and the admin dashboard just displays it. GET /cars (backend/src/routes/cars.js:28-40) sorts only by listed_at/price/mileage/year/views with no featured ordering. The only 'fee' code in the repo is the unrelated rentals airport pickup_fee (rentals.js, rental_bookings). No implementation of certification fee, success commission, or featured-listing structures exists in backend/src, admin/src, src/api, or src/context.

### 13. No price_history table — sparklines, Price Drop badge, and relist pricing have no data source  `[schema]`
**Evidence:** cars.price is a single INT with no audit trail (schema.sql); VehicleDetailScreen's sparkline/market-diff and CarListCard badges fabricate data client-side (src/data/marketData.js getPriceHistory/getMarketDiff, keyed to mock ids 1–25); server-side price-drop detection (gap above) is impossible without it. Needs price_history(car_id, price, changed_at, reason) plus a price-change endpoint that writes it.

**Verified:** Gap confirmed. backend/src/schema.sql defines cars.price as a single INT (line 38) with no price_history table anywhere; the only 'price_drop' string in backend is a comment listing notification types (line 142). backend/src/routes/cars.js has no route that updates price at all — its only mutations are POST / (create), POST /save/:id, and PATCH /:id/status (status only) — so no price change can occur server-side, let alone be audited. All buyer-facing price-history UI is fabricated client-side: src/data/marketData.js hardcodes PRICE_DROPS for mock ids 1–25 and getPriceHistory() synthesizes a 6-point sparkline series arithmetically from the current price (market avg invented as price*1.08); VehicleDetailScreen.js (lines 65–66, 244–255) and SavedScreen.js consume these fakes. Relist pricing is local-only — src/context/AppContext.js relistSubmission (line 835) carries the comment 'Local relist update since backend does not support seller edit routes'. No server-side price_drop notification insert exists anywhere in backend/src, and admin/src has no price-edit or history UI. The proposed price_history(car_id, price, changed_at, reason) table plus a price-change endpoint is genuinely missing.

### 14. Admin listing lifecycle broken/missing: 'Remove' always 400s, no archived view, no listing-edit endpoint, no photo delete/reorder  `[admin]`
**Evidence:** admin/listings/page.tsx:135 sends status 'removed' but cars.js:209 allowlist is [...'archived'] → button always fails with 400; no UI filter for 'archived' so archived cars are unviewable. No PATCH /cars/:id exists for price/spec/description edits (blueprint price-drop/relist flows can't be done). POST /inspections/cars/:carId/photos only array_cat-appends — no DELETE/reorder endpoint, so wrong uploads are permanent; upload URLs are baked from req.protocol+Host with no 'trust proxy'.

**Verified:** All parts verified in code. (1) admin/src/app/(admin)/listings/page.tsx:135 sends status 'removed' but backend/src/routes/cars.js:209 allowlist is ['under_review','scheduled','inspecting','live','reserved','sold','archived'] — 'removed' absent, so the Remove button always gets 400 'Invalid status' (shown via alert). (2) The UI STATUSES filter (page.tsx:7) omits 'archived', so archived cars are unviewable from the dashboard — nuance: backend GET /admin/listings (admin.js:79-81) does accept status=archived, so this is a UI-only gap, but as claimed there is no UI filter. (3) No PATCH /cars/:id exists — cars.js has only PATCH /:id/status; admin.js has only 3 GET routes; no endpoint anywhere edits price/spec/description, and mobile src/api/cars.js has no edit call (relist is client-side mock). (4) POST /inspections/cars/:carId/photos (inspections.js:63-80) only array_cat-appends to cars.images; the sole DELETE route in the backend is saved-searches.js:55; the admin photos page and api.ts have no delete/reorder — wrong uploads are permanent. (5) inspections.js:68 builds baseUrl from req.protocol + req.get('host'); no 'trust proxy' setting exists anywhere in backend/server.js, so behind the planned Nginx/TLS proxy http:// URLs would be permanently persisted. No refuting implementation found in backend/src, admin/src, src/api, or src/context.

### 15. Handover booking race conditions and validation holes  `[backend-route]`
**Evidence:** POST /handovers: live-check and reserve are not transactional (two buyers can reserve the same car), no self-purchase check, booking_id from Date.now().toString(36) can collide; POST /rentals/:id/book has the same overlap-check/insert race plus no past-date/max-days validation. Marking sold via PATCH /cars/:id/status doesn't increment completed_sales while handover-confirm does — two inconsistent 'sold' paths. Needs transactions/row locks and unified sold semantics.

**Verified:** All sub-claims verified in code. backend/src/routes/handovers.js:14-36 does live-check SELECT, INSERT, then unconditional UPDATE to 'reserved' as three separate pool.query calls — no BEGIN/FOR UPDATE anywhere in backend/src (grep: zero hits), so two concurrent buyers can both reserve; no req.user.id vs car.seller_id self-purchase check; booking_id 'BK-'+Date.now().toString(36) (line 22) is millisecond-derived — schema.sql:102 UNIQUE constraint turns a same-ms collision into a 500 rather than a duplicate (nuance, still a hole, no retry). rentals.js:124-150 has the same overlap-SELECT-then-INSERT race with no transaction and no EXCLUDE constraint in schema.sql, and validation (lines 107-121) checks only presence/days>=1/min_days — past start_date accepted, no max-days cap. Sold semantics are split: cars.js:207-222 PATCH /cars/:id/status sets sold+sold_at without incrementing completed_sales, while handovers.js:126-134 confirm does increment it; both paths are reachable from the admin UI (listings page Mark Sold via admin/src/lib/api.ts:70 vs handovers confirm), so trust-score sales counts diverge. No compensating code exists in admin/src, src/api, or src/context.

## MEDIUM — needed before a real pilot

### 1. Admin mobile surfaces ignore their live endpoints: Inspections tab and AdminAnalyticsScreen are hardcoded  `[mobile-wiring]`
**Evidence:** AppContext adminInspections is immutable INITIAL_INSPECTIONS_ADMIN (5 fake appointments) — GET /inspections is never called, and submitInspectionForm fires POST /inspections/:id/complete against mock ids i1–i5 which 404. AdminAnalyticsScreen hardcodes FUNNEL/TOP_MAKES/CENTERS and doesn't even import useApp, while GET /admin/analytics and /admin/stats are built and smoke-tested.

**Verified:** Gap confirmed on all points. (1) src/context/AppContext.js:122 declares `const [adminInspections] = useState(INITIAL_INSPECTIONS_ADMIN)` with no setter — permanently the 5 mock appointments i1–i5 defined at lines 61–67; no code in src/ ever calls GET /inspections even though backend/src/routes/inspections.js:9 implements it (requireAdmin list). (2) AppContext.js:994 `submitInspectionForm` POSTs /inspections/{id}/complete with those mock ids (passed from AdminPanelScreen.js:241 InspectionsTab); the backend handler (inspections.js:110) queries a UUID primary key (schema.sql:82), so 'i1' fails unconditionally (invalid-uuid error, arguably 500 rather than 404 — strictly worse) and the error is swallowed by console.warn. (3) src/screens/AdminAnalyticsScreen.js:12–31 hardcodes FUNNEL/TOP_MAKES/CENTERS and imports neither useApp nor any api client, while GET /admin/stats (backend/src/routes/admin.js:8) and GET /admin/analytics (admin.js:37) exist unused by mobile. src/api/ contains no admin module. The only consumers of these endpoints are in admin/src (the web dashboard), which does not cover the admin mobile surfaces the claim is about.

### 2. /cars payload missing the fields the app's market-data layer needs; screens degrade silently for real cars  `[backend-route]`
**Evidence:** Home 'fresh/popular' rails, SavedScreen badges, MapScreen neighborhoods, and every CarListCard read NEIGHBORHOOD_MAP/LISTED_DAYS_AGO/SAVED_COUNTS/PRICE_DROPS/DRIVE_TYPES from src/data/marketData.js keyed to mock ids '1'–'25' — API cars all tie at defaults. Backend already stores location, listed_at, saves, drive_side; work is (a) expose/compute listed-days, save counts, market avg/diff (rec #6), neighborhood in GET /cars and (b) rewire the components off the mock maps. Also add drive_side as a GET /cars filter (rec #19 — filter absent).

**Verified:** CONFIRMED with two caveats. Core gap is real: backend cars have UUID ids (backend/src/schema.sql:27) while src/data/marketData.js maps are keyed '1'-'25', so every API car falls to defaults ('Kigali', 14 days, 0 saves, 0 drop, 'LHD'). Verified consumers read only these mock helpers, never API fields: HomeScreen.js:94-95 (fresh/popular rails — all ties, sort becomes fetch order), SavedScreen.js:13-15 (badges), MapScreen.js:77,83 (API cars resolve to 'Kigali', matching none of the 6 neighborhoods — map shows zero cars with real data), CarListCard.js:21-22,54, VehicleDetailScreen.js:63-70. No reads of car.saves/car.location/car.drive_side/car.listed_at in any of these files. mapCar (src/context/AppContext.js:236-267) drops listed_at and references a nonexistent below_market column, so getMarketDiff yields a uniform fabricated -7% for all API cars; no market avg/diff code exists in backend/src. Caveats refuting parts of the proposed work: (1) drive_side IS already a GET /cars filter — backend/src/routes/cars.js:26 — only the frontend filter UI/wiring is absent (rec #19 is frontend work, not a backend route gap); (2) part (a) is overstated: GET /cars uses SELECT c.* so location, saves, views, drive_side, listed_at are already exposed — the only missing backend computation is market avg/diff (rec #6); the bulk of remaining work is (b) rewiring the five components off the mock maps plus passing listed_at through mapCar.

### 3. 36-angle photo standard has no structural backing — images are an unordered TEXT[]  `[schema]`
**Evidence:** schema.sql cars.images is a flat TEXT[]; the Encar-style 36-angle standard (CLAUDE.md Design System) and admin photos page's 26-slot guide (which ticks by upload count, not angle) have no car_photos(car_id, angle_key, url, sort_order, is_defect) model. Also no video_url for the committed video walkaround (rec #13). Needed before the photography standard is enforceable.

**Verified:** Confirmed. backend/src/schema.sql:43 defines cars.images as a flat TEXT[] with no car_photos(car_id, angle_key, url, sort_order, is_defect) table anywhere in the schema (verified against the full CREATE TABLE list). The upload route (backend/src/routes/inspections.js:63-77) just array_cat's bare URLs onto cars.images in upload order with no angle metadata. The admin photos page (admin/src/app/(admin)/listings/[id]/photos/page.tsx) defines 26 named PHOTO_GUIDE_SLOTS but ticks them purely by upload count (`isMatched = currentCount > itemIndex`, currentCount = car.images.length), so any 26 files satisfy all slots. Repo-wide grep for angle_key/sort_order/is_defect/video_url across backend, admin/src, src/api, src/context returns zero hits; no video_url exists for the rec #13 video walkaround. The photography standard has no structural backing to enforce.

### 4. Inspection centers and slot capacity are unmodeled — double-booking is possible and centers are hardcoded in 3 places  `[schema]`
**Evidence:** center is free TEXT on inspections/handovers/submissions/rental_bookings; the 3 Kigali centers are hardcoded in InspectionSchedulingScreen (CENTERS/TIME_SLOTS), admin/inspections/page.tsx, and admin filters. No inspection_centers table, no slot-availability endpoint, no capacity check in PATCH /submissions/:id/schedule (and seller self-schedule bypasses admin review while writing schedule fields onto submissions, diverging from the inspections table the admin path writes). Needs a centers table + availability endpoint + single source of truth for the appointment.

**Verified:** Gap is real. No inspection_centers table exists (backend/src/schema.sql lists 13 tables; center is free TEXT at schema.sql:86 inspections, :106 handovers, :196 submissions.inspection_center, :263 rental_bookings — the 3 center names live only in a SQL comment). No slot-availability endpoint exists (backend/src/routes/inspections.js exposes only admin list, report, and detail GETs; the only availability logic in the backend is rentals per-car date overlap at rentals.js:134). PATCH /submissions/:id/schedule (backend/src/routes/submissions.js:139-180) performs zero capacity/conflict checking — arbitrary center/date/time strings are accepted, so unlimited double-booking of the same slot is possible; same for the admin PATCH and POST /handovers. Centers are hardcoded client-side in src/screens/InspectionSchedulingScreen.js:11 (CENTERS, with display-only slots counts) and :59 (TIME_SLOTS), and admin/src/app/(admin)/inspections/page.tsx:7 (CENTERS filter array), plus more hardcodings in AppContext mock data. The divergence claim also holds with nuance: the seller self-schedule path (requireAuth, allowed from under_review, bypassing admin review) writes schedule fields onto submissions AND inserts an inspections row, while the admin path never writes submissions schedule columns and only inserts an inspections row if center is passed — which the admin web UI (admin/src/app/(admin)/submissions/page.tsx:126) never passes, so admin-side scheduling creates no inspection record at all. Additionally, inspections has no UNIQUE(submission_id), making both ON CONFLICT DO NOTHING inserts no-ops, so rescheduling creates duplicate inspections rows. No refuting implementation found anywhere in backend/src, admin/src, src/api, or src/context.

### 5. Seller-facing endpoints missing: analytics, relist/price-edit, seller-side handover view  `[backend-route]`
**Evidence:** SellerAnalyticsScreen is pure mock (MOCK_LISTING_ANALYTICS, VIEWS_DATA) — only admin analytics routes exist, and only lifetime counters (cars.views/saves) are stored, so views/day charts also need an events time-series or daily rollup table. AppContext relistSubmission/updateSubmissionPrice are pure local ('backend does not support seller edit routes' comment). GET /handovers/my is buyer-only — a seller cannot see handovers booked against their cars, and buyer cancel doesn't notify the seller (handovers.js cancel path).

**Verified:** Gap confirmed on all three points. (1) Seller analytics: only admin-gated analytics exist (backend/src/routes/admin.js lines 8/37, requireAdmin); SellerAnalyticsScreen.js uses hardcoded MOCK_LISTING_ANALYTICS/VIEWS_DATA (lines 14/24) with no API calls, and schema.sql stores only lifetime views/saves counters on cars (lines 48-49) — no events or daily-rollup table exists, so views/day charts have no data source. (2) Relist/price-edit: the only seller-writable route is PATCH /submissions/:id/schedule (inspection booking, not price); car price mutations are admin-only (cars.js lines 169/207); AppContext.js updateSubmissionPrice (line 818) and relistSubmission (line 835) are pure local setState — line 836 states "backend does not support seller edit routes". (3) Seller handover view: GET /handovers/my filters WHERE h.buyer_id = $1 (handovers.js), the only list alternative is admin-only, and the buyer cancel path updates status with no seller notification insert. Minor nuance: the handover *booking* path does notify the seller once, but no seller-side view/tracking endpoint exists and cancellation is silent.

### 6. Rentals vertical incomplete server-side: no admin CRUD, honor-system check-in/return, no condition-photo upload  `[backend-route]`
**Evidence:** rental_cars has no POST/PATCH/DELETE — fleet exists only via src/seed-rentals.js; PATCH /rentals/bookings/:id/status lets the renter self-activate and self-complete their own rental (code comment admits staff confirmation is unmodeled) with no notifications or cancellation cutoff; RentalCheckInScreen's walkaround photos are demo toggles with no upload endpoint; rental_cars.rating has nothing populating it (no rental_reviews table). Feature is also absent from CLAUDE.md (undocumented surface).

**Verified:** Gap confirmed. backend/src/routes/rentals.js has no rental_cars CRUD (only GET fleet/detail, POST book, GET bookings, PATCH booking status); fleet exists solely via backend/src/seed-rentals.js, and admin/src contains zero rental references. PATCH /rentals/bookings/:id/status (rentals.js:171) is renter-scoped (WHERE renter_id = req.user.id) allowing self-activate/self-complete; the lines 169-170 comment admits staff confirmation is unmodeled; no notifications on transitions and no cancellation cutoff. RentalCheckInScreen.js even submits a hardcoded client-side STAFF_RECORD mock (lines 12-18) as the condition record, and its walkaround photos are boolean toggles (line 109) sending only slot names — no upload endpoint or multer exists for rentals. rental_cars.rating is seeded-only; no rental_reviews table and reviews.js touches only marketplace handover reviews. Minor nuance: CLAUDE.md is not literally silent — it has a stale unchecked Phase 6D item calling the vertical "client-side mock" — but the shipped backend surface is undocumented. No refuting implementation found in backend/src, admin/src, src/api, or src/context.

### 7. Vehicle history endpoint returns hardcoded placeholders with no backing data  `[backend-route]`
**Evidence:** GET /cars/:id/history hardcodes rra_duty_paid=true and accident_history=false (explicit 'Placeholder — connect API' comments), derives import_origin from drive_side, and equates mileage_verified with 'an inspection exists'. Excluding the RRA/insurance integrations themselves, the schema still needs the fields (previous_owners, import_origin/year, mileage_verified, insurance_active — none exist; cars.vin isn't even UNIQUE) and admin input paths so the endpoint can serve real data instead of the mobile screen's mock (which it currently doesn't even call).

**Verified:** Gap confirmed. GET /cars/:id/history (backend/src/routes/cars.js:51-101) hardcodes rra_duty_paid=true and accident_history=false with explicit 'Placeholder — connect ... API when available' comments, derives import_origin solely from drive_side, equates mileage_verified with the mere existence of a completed inspection row, and counts on-platform handovers as previous_owners (0 for any new listing). The cars table (backend/src/schema.sql:26-53) has none of the needed columns (previous_owners, import_origin/year, mileage_verified, insurance_active) and vin is plain TEXT with no UNIQUE constraint or index. No admin input path exists: admin/src never references these fields and POST /cars accepts only vin. The mobile client defines but never calls getVehicleHistory (src/api/inspections.js:10 has zero callers); VehicleHistoryScreen.js renders the static VEHICLE_HISTORY mock from src/data/inspectionData instead. No refuting implementation found.

### 8. Missing account endpoints: profile update, password reset, Google OAuth, admin user search  `[backend-route]`
**Evidence:** No PATCH /auth/me, avatar upload, or password reset exists; Google OAuth is committed in CLAUDE.md tech stack ('email + Google OAuth') but only email register/login exist and schema has no google_id/oauth_provider column (password_hash comment says 'NULL for Google OAuth users'). Admin side: no GET /users search/list — /listings/new requires pasting a raw seller UUID (typo → FK 500) and the 'Users' page can only show the ID queue.

**Verified:** Gap confirmed on all four points. (1) backend/src/routes/auth.js has only POST /register, POST /login, GET /auth/me — no PATCH /auth/me; avatar_url exists in schema (schema.sql:21) but no route ever writes it (all UPDATE users statements are side-effects: id_verified, trust_score, completed_sales). (2) No password reset anywhere; src/screens/SignInScreen.js:52 stubs it with a fake Alert claiming a reset link was sent, calling no API. (3) Google OAuth: sole reference is the schema comment at backend/src/schema.sql:11 ('NULL for Google OAuth users'); no google_id/oauth_provider column, no OAuth route, no client Google sign-in. (4) No GET /users list/search: admin.js exposes only /stats, /analytics, /listings; id-verification.js only /queue and PATCH /:userId; admin users page (admin/src/app/(admin)/users/page.tsx) renders only the ID queue, and admin/src/app/(admin)/listings/new/page.tsx:122-130 requires typing a raw seller UUID into a free-text field (placeholder 'e.g. uuid-of-seller') with no lookup or validation.

### 9. API hardening pass: no transactions anywhere, no input validation, unclamped pagination, CORS misconfig, multer errors as 500s  `[ops]`
**Evidence:** Backend-wide: zero DB transactions across multi-step writes; POST /cars accepts undefined title/price/seller_id and trusts client-sent inspection_score; GET /cars and GET /admin/listings parseInt limit/offset with no clamp (non-numeric → NaN → SQL 500); GET /cars status param is dead code behind a misleading admin comment; server.js CORS default '*' with credentials:true is rejected by browsers for credentialed requests; multer fileFilter errors surface as generic 500s; no rate limiting on /auth/login. One structured pass with a validation lib + transactions closes most route-level footguns.

**Verified:** All seven evidence points verified in code. (1) Zero transactions: grep for BEGIN/COMMIT/ROLLBACK across backend/src returns nothing; multi-step writes in backend/src/routes/cars.js POST / (lines 174-197: INSERT car + UPDATE submissions + UPDATE inspections) and handovers.js PATCH /:id/confirm (lines 113-145: 4 sequential writes) run without transactions. (2) POST /cars (cars.js:169-183) destructures req.body straight into INSERT — undefined title/price/seller_id accepted, client-sent inspection_score trusted. (3) cars.js:31 and admin.js:91 push parseInt(limit)/parseInt(offset) with no NaN guard or clamp — non-numeric input yields NaN → pg error → 500. (4) cars.js:13-14 comment claims status is "honoured for admins" but ['live'].includes(status) only ever yields 'live' and the route has no auth check — dead code with misleading comment. (5) server.js:12-16 defaults origin to '*' with credentials:true — browsers reject credentialed requests with wildcard origin. (6) upload.js:25 fileFilter rejects with plain Error; no MulterError handler exists anywhere, so it hits the generic 500 handler in server.js:57-60. (7) No rate-limit dependency in backend/package.json and no throttling on auth.js POST /login; no validation library (joi/zod/express-validator) exists in the project. Nothing refutes the claim — the gap is genuine.

### 10. Guest data exposure inconsistent with role spec  `[backend-route]`
**Evidence:** GET /cars/:id (public) returns seller_phone to unauthenticated guests — spec says guests cannot contact sellers, and mobile uses it for direct WhatsApp contact; GET /inspections/report/:carId exposes raw VIN publicly; view counter increments on every fetch with no dedup (admin photos page visits inflate analytics, noted in admin/listings/[id]/photos which fetches the public route). Decide gating and dedup views.

**Verified:** Confirmed on all three points; no refuting implementation exists. (1) GET /cars/:id (backend/src/routes/cars.js:105-124) has no auth middleware and selects u.phone AS seller_phone (line 108); middleware/auth.js has only requireAuth/requireAdmin, no optional-auth field stripping. Mobile consumes it for ungated direct WhatsApp: AppContext.js:240 maps seller_phone→sellerPhone and VehicleDetailScreen.js:86-89 uses it in contactWhatsApp invoked at lines 305/439 WITHOUT executeWithAuth (in-app Chat at line 310 is gated) — violating the spec's 'Guests cannot message anyone'. (2) GET /inspections/report/:carId (inspections.js:41-59) is public and returns c.vin raw; /cars/:id/history and /cars/:id (c.*) also expose vin; no masking/redaction code anywhere in backend/src. (3) cars.js:119 increments views unconditionally on every fetch with no dedup (no view-log table in schema.sql, no role exclusion), and the admin photos page (admin/src/app/(admin)/listings/[id]/photos/page.tsx:59 via admin/src/lib/api.ts:68 getCar) fetches the same public /cars/:id route, so admin visits inflate analytics. Gating decision and view dedup remain unbuilt.

### 11. Admin /listings/new semantic bugs: USD/RWF currency mismatch and cars born 'live' with zero photos  `[admin]`
**Evidence:** admin/listings/new/page.tsx labels price 'Price (USD)' but pre-fills the submission's RWF asking_price and every other page renders cars.price as RWF; POST /cars hardcodes status 'live' with images:[] so listings are publicly live before any photo upload (no draft state), and 'Publish & Exit' on the photos page is navigation-only. Needs a draft→live publish step and one currency convention.

**Verified:** Confirmed with one direction correction. (1) Live-with-zero-photos: backend/src/routes/cars.js:179 hardcodes status 'live' in the POST /cars INSERT; admin/src/app/(admin)/listings/new/page.tsx:98 sends images:[]; public browse (cars.js:14) serves live cars only, so the photo-less listing is instantly buyer-visible. No 'draft' status exists anywhere — schema.sql has none and the PATCH /cars/:id/status allowlist (cars.js:209) omits it; inspections.js:143 is a second hardcoded-live path. (2) 'Publish & Exit' on admin/.../listings/[id]/photos/page.tsx:136-141 is literally router.push('/listings') — no API call, so it is navigation-only as claimed. (3) Currency mismatch is real but the claimed direction is inverted: the app-wide base convention is USD (CarSubmissionScreen.js:223 collects 'Your Asking Price (USD)', CarCard.js:23 renders $price, marketData.js formatRWF multiplies USD by 1300, mock prices ~$25-39k), and new/page.tsx:262's 'Price (USD)' label is consistent with that; the mislabeled pages are admin submissions/page.tsx:95 and listings/page.tsx:101, which render the same USD-base numbers as 'RWF ...' (off ~1300x). Either way two contradictory conventions coexist for the identical value, no currency column/conversion exists, so the 'one currency convention + draft-to-live publish step' gap genuinely remains.

### 12. Notification generation and delivery scaffolding incomplete  `[backend-route]`
**Evidence:** GET /notifications is a hardcoded LIMIT 50 with no pagination/unread filter; several blueprint notification types (price drop, saved-search match, listing status changes beyond the few wired) have no generators; no push-token storage exists (schema has no push_tokens table / users.expo_push_token) so the 6D 'wire Expo Notifications' item has nothing to register against; socket send_message notifies even actively-chatting recipients. (OneSignal/WhatsApp delivery itself excluded as Phase 8.)

**Verified:** All four evidence points verified in code. (1) backend/src/routes/notifications.js:10-14 is a bare SELECT with hardcoded LIMIT 50 — no pagination params, no unread filter, no unread-count route. (2) All 14 INSERT INTO notifications sites emit only new_message/handover/listing_update; schema.sql's notifications table even documents price_drop as a type but no generator exists; cars.js has zero notification code so price changes and live-listing creation never notify savers or saved-search watchers, and saved_searches.notify_enabled is consumed by nothing outside its own CRUD. (3) No push_tokens table and no users.expo_push_token in schema.sql; no expo-server-sdk/OneSignal/push-token code in backend, src/api, src/context, admin, or any package.json — nothing to register Expo push tokens against. (4) backend/src/socket.js:57-63 inserts a notification unconditionally despite its own 'if they're not in the room' comment — no room-membership check. Minor nuance: submission-pipeline status changes (received/scheduled/rejected/live, inspection complete) ARE wired, but the claim already scopes this as 'beyond the few wired'. Gap is genuine.

## LOW — post-pilot

### 1. Inspection 'in_progress' state is unreachable — no start-transition endpoint  `[backend-route]`
**Evidence:** schema.sql anticipates inspections.status 'in_progress' and admin/inspections/page.tsx offers an 'In Progress' filter (querying 'inspecting'), but no backend code ever moves an inspection out of 'scheduled' before completion — filter always empty, 'Continue' button unreachable, and 'Mark Inspecting' on submissions only touches the submission row. Needs PATCH /inspections/:id/start (sets started_at) and status-name alignment.

**Verified:** Confirmed. backend/src/routes/inspections.js has only GET /, GET /report/:carId, POST /cars/:carId/photos, GET /:id, and POST /:id/complete — the sole status write jumps 'scheduled' -> 'complete' (line 134); no PATCH /inspections/:id/start exists and started_at (schema.sql line 90) is never written anywhere in the repo. The 'in_progress' string exists only as a schema comment. admin/src/app/(admin)/inspections/page.tsx line 75 filters on status='inspecting', a value inspections rows can never hold (name mismatch with the schema's 'in_progress' on top of the missing transition), so the filter is always empty and the 'Continue' label branch (line 108) is unreachable. The submissions-page 'Mark Inspecting' button (submissions/page.tsx line 142) hits PATCH /submissions/:id which per backend/src/routes/submissions.js only updates the submissions row — it never touches the inspections table. Mobile (src/api/inspections.js, src/context/AppContext.js:994) and admin api client (admin/src/lib/api.ts) likewise have no start call. The gap is real: a PATCH /inspections/:id/start (setting status and started_at) plus status-name alignment ('in_progress' vs 'inspecting') is needed.

### 2. Growth features built as screens with zero backend: referrals, dealer accounts, disputes/7-day return  `[schema]`
**Evidence:** ReferralScreen fabricates codes client-side — no referral routes, no users.referral_code/referrals table; DealerProfileScreen uses a hardcoded DEALERS map — no 'dealer' role (users.role is buyer|seller|admin), no dealer_profiles/subscription/commission-tier model; rec #30 disputes and the agreed 7-day return guarantee have no disputes table, no return_deadline on handovers, and no admin mediation routes. Also no admin chat-monitoring endpoint despite 'Monitor all chats' in the role spec.

**Verified:** Gap confirmed on all points. Referrals: ReferralScreen.js:29 fabricates the code client-side ('INZ-' + name prefix + '2026'); zero referral matches in backend/src, admin/src, src/api, src/context — no routes, no users.referral_code, no referrals table in schema.sql. Dealers: DealerProfileScreen.js:10 hardcoded DEALERS map; schema.sql:12 limits role to buyer|seller|admin and auth.js:23 rejects any role outside ['buyer','seller']; no dealer_profiles/subscription/commission tables. Disputes/7-day return: no disputes table; handovers table (schema.sql:100-116) has no return_deadline; the only backend mention is notification copy text in handovers.js:144 — the return_record JSONB at schema.sql:271 belongs to rental_bookings (rental check-in), not purchase returns; no mediation routes. Admin chat monitoring: admin.js exposes only /stats, /analytics, /listings; messages.js routes are all participant-scoped requireAuth — no admin conversation-monitoring endpoint.

### 3. Valuation/AI-price endpoint missing; blueprint pricing tools are client-side formulas  `[backend-route]`
**Evidence:** CarSubmissionScreen Step 3 'AI Price Suggestion' and CarValuationScreen both run estimateValuation() in src/data/finance.js over in-memory cars; rec #21 commits a comparables-based backend suggestion. VIN decoder (rec #4) also has no endpoint. Low urgency — the client versions function as estimates — but they're committed as backend features.

**Verified:** Confirmed. No valuation/AI-price or VIN-decoder endpoint exists: grep for valuation/price-suggest/comparables across backend/src, admin/src, src/api, and src/context returned zero hits, and the backend route inventory (backend/src/routes/: admin, auth, cars, handovers, id-verification, inspections, messages, notifications, rentals, reviews, saved-searches, submissions) has no such route — cars.js/admin.js expose only browse/detail/history/save/create/status and stats/analytics/listings. The pricing tools are exactly as claimed: src/data/finance.js:17 estimateValuation() is a local formula (make-filtered average of in-memory cars × year/mileage depreciation, ±8% band) consumed by CarSubmissionScreen.js:100 (Step 3 "AI Price Suggestion", Beta badge) and CarValuationScreen.js:26, with no API call in any src/api module. VIN appears in backend only as a stored/displayed text column (backend/src/schema.sql:41, cars.js insert/select, admin display pages) — no decode/lookup endpoint. Client versions function as estimates, but recs #21 and #4 have no backend implementation.

### 4. Schema housekeeping: no CHECK constraints, no updated_at, saves-counter drift, display-TEXT dates  `[schema]`
**Evidence:** schema.sql: no CHECK on any status column (invalid strings persist silently — already happening via unvalidated PATCH /submissions/:id), no updated_at anywhere, cars.saves denormalized beside saved_cars with no trigger (POST /cars/save/:id is non-atomic check-then-insert), scheduled/handover dates stored as display TEXT alongside real timestamps (submissions PATCH builds new Date(`${date} ${time}`) unguarded → Invalid Date insertable), reviews lacks UNIQUE(handover_id), cars.vin not UNIQUE. One migration pass plus route fixes.

**Verified:** Gap confirmed against backend/src/schema.sql and routes. Only CHECK in the entire schema is reviews.rating (schema.sql:174); all 8 status/enum TEXT columns are unconstrained. PATCH /submissions/:id (routes/submissions.js:86-96) writes req.body.status with no whitelist (while PATCH /cars/:id/status at routes/cars.js:209-212 does validate — pattern exists, not applied). Zero occurrences of updated_at in backend. POST /cars/save/:id (cars.js:127-142) is non-atomic SELECT-then-mutate with a separate saves counter UPDATE, no transaction and no trigger anywhere — drift possible. Display TEXT dates confirmed: inspections.scheduled_date/time TEXT beside scheduled_at TIMESTAMPTZ (schema.sql:87-89), handovers.handover_date/time as display strings with no real slot timestamp (107-108), submissions.inspection_date/time TEXT (197-198). Admin PATCH builds new Date(`${date} ${time}`) unguarded (submissions.js:102-103); the seller /schedule route does guard with isNaN (156-157), and pg actually rejects an Invalid Date param, so the practical failure is a 500 with a half-applied non-transactional update rather than a stored bad timestamp — mechanism nuance, defect still real. reviews has no UNIQUE(handover_id) and reviews.js POST has no duplicate check — repeat reviews insert and re-bump trust_score. cars.vin (schema.sql:41) has no UNIQUE/index. No migration framework or ORM provides these constraints elsewhere; db-init.js just runs schema.sql. Minor caveats: cars-status whitelist and the seller-route date guard partially mitigate two sub-points at the app level, but the specific cited routes and all schema-level claims hold.

### 5. Stale project docs: CLAUDE.md contradicts the built admin surface and carries a superseded Supabase plan  `[ops]`
**Evidence:** CLAUDE.md marks '/cars/new — NOT BUILT' and 'File upload UI for 36-angle photos — NOT BUILT', but admin/src/app/(admin)/listings/new/page.tsx and listings/[id]/photos/page.tsx both exist and work; the old Phase 6 Supabase checklist is fully unchecked but superseded by the built Express/Postgres stack; the /rentals vertical (7 routes, 2 tables, seed script) is absent from the doc entirely. Reconcile so future work isn't planned against a false map.

**Verified:** Confirmed, but with corrected evidence. The cited 'NOT BUILT' markers do not exist: CLAUDE.md lines 773/780 already mark /listings/new and /listings/[id]/photos as [x] built (fixed in commit 0ebe40b), matching admin/src/app/(admin)/listings/new/page.tsx and listings/[id]/photos/page.tsx — that part of the claim is refuted. Rentals is also not 'absent entirely' (line 754 mentions it). However genuine staleness remains: (1) CLAUDE.md lines 409-424 still carry the fully-unchecked superseded Supabase Phase 6 plan, contradicting the built Express/Postgres backend documented as complete in sections 6B/6C of the same file; (2) line 754 still says the rental vertical 'is client-side mock' although commit b565553 (Jul 18, after the last CLAUDE.md edit) built backend/src/routes/rentals.js (6 routes, not 7), rental_cars + rental_bookings tables in backend/src/schema.sql (lines 230/255), backend/src/seed-rentals.js, and mobile src/api/rentals.js; (3) the 6D item 'Mobile modules for inspections report / reviews / saved-searches' is stale — src/api/inspections.js and src/api/reviews.js now exist. Doc reconciliation is warranted for the Supabase section and the rentals/mobile-modules checklist lines, not for the admin listing/photos pages.
